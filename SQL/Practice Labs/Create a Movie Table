CREATE TABLE Movie (
   ID SMALLINT unsigned,
   Title VARCHAR(50),
   Rating CHAR(4),
   ReleaseDate DATE,
   Budget DECIMAL(8, 2)
);

/* Create a Movie table with the following columns
ID, positive integer with a max value of 50,000
Title, variable-length string with up to 50 characters
Rating, fixed-length string with 4 characters
ReleaseDate, year, month, and day
Budget - decimal value representing a max of 999,999 with 2 digits for cents. 
(For budget we need to use 8 to account for the $ and comma.) */